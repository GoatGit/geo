# 安全政策

## 已知历史泄露(处置要求)

历史提交(即 `9d23678` 的父提交)曾包含 **RDS 数据库密码**与 **AgentBay API Key**,且该提交**已推送至远端**。处置要求:

1. **立即轮换凭据**(必须):在阿里云控制台重置 RDS 密码、吊销并重发 AgentBay API Key。历史已推送,凭据必须视为已泄露,轮换不可省略。
2. **如需从历史中彻底清除**(可选,轮换之后):使用 `git filter-repo` 重写历史并强制推送所有分支,同时通知所有协作者重新克隆。仅删除文件不足以移除历史中的内容。

## 凭据管理约定

- 所有凭据(数据库、Redis、OSS、AgentBay、短信、支付等)**只经环境变量注入**,由部署平台(SAE 环境变量/密钥)或本地 `.env*` 文件提供。
- `.env*` 已被 `.gitignore` 排除,**任何密钥不得写入源码、脚本、文档或提交信息**。
- 生产凭据与开发凭据分离,开发环境不持有生产库写权限。

## 漏洞报告

发现安全漏洞请联系:**见仓库所有者**。
