{
  "name": "@geo/browser-session",
  "version": "0.1.0",
  "description": "远程浏览器会话代理:AgentBay Browser Use(broker_latest 镜像,CDP)与 Mock 实现(docs/07 §4)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
