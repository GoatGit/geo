{
  "name": "@geo/metrics",
  "version": "0.1.0",
  "description": "口径引擎:提及抽取/品牌匹配/综合名次/可见性漏斗/健康阈值/问题分层/行动清单规则(docs/02 全量代码化)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@geo/shared": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
