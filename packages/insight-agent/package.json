{
  "name": "@geo/insight-agent",
  "version": "0.1.0",
  "description": "Insight Agent(docs/09):LLM 判定层——品牌识别/口碑/分类/拓写,双协议(openai/anthropic)结构化调用,规则引擎为调用方降级路径",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@geo/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
